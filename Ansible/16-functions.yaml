#functions == filters

- name: functions
  hosts: local
  connection: local
  vars:
    technologies: DevOps, Linux, Ansible # one string
    name: "Vikas"
    gmail: Info@GMAIL.COM
    ipaddress: "19222.168.0.1"
    # dictionary == map
    tags:
      Application: payment
      Environment: dev
    # 
    listTags:
    - key: Application
      value: payment
    - key: Environment
      value: dev
  tasks:
  - name: default variable
    ansible.builtin.debug:
      msg: "Hello, {{ name | default('Ramesh') }}"
    
  # string to list
  - name: convert string to list
    ansible.builtin.debug:
      msg: "Technologies are: {{ technologies | split(',') }}"
      #output ['DevOps', 'Linux', 'Ansible']
#split() is used to convert a string into a list by separating it using a delimiter (separator).
  - name: convert map to list
    ansible.builtin.debug:
      msg: "Tags are: {{ tags | dict2items }}"
      #list of dictonaries
 #dict2items is an Ansible filter used to convert a dictionary into a list of key-value pairs.   
    #[{'key':'Application', 'value': 'Payment'}, {'key': 'Environment', 'value': 'dev'}]

  - name: convert list into map
    ansible.builtin.debug:
      msg: "tag map: {{ listTags | items2dict }}"
   #tag map: {'Application': 'payment', 'Environment': 'dev'}
   #items2dict converts it into a single dictionary



#==============================================
# read a YAML file (student.yaml) and convert it into an Ansible variable
  - name: read yaml and take it into variable
    ansible.builtin.set_fact:
      student_yaml: "{{ lookup('file', 'student.yaml') | from_yaml }}"
    #When the task executes, the file is read, converted, and stored into a new variable using set_fact.
    #ie sutdent_yaml

  - name: convert yaml into json
    ansible.builtin.debug:
      msg: "json: {{ student_yaml | to_json  }}"

  - name: save json to file
    ansible.builtin.copy:
      dest: ./student-data.json
      content: "{{ student_yaml | to_nice_json  }}"
#===================================================

  - name: convert uppercase
    ansible.builtin.debug:
      msg: "{{ name | upper }}"

  - name: convert lowercase
    ansible.builtin.debug:
      msg: "{{ gmail | lower }}"

  - name: check IP is valid or not
    ansible.builtin.debug: 
      msg: "{{ ipaddress | ansible.utils.ipv4 }}"

      #pip3.9 install netaddr use this when asked

  - name: grep for root
    #ansible.builtin.command: cat /etc/passwd | grep root
    ansible.builtin.shell: cat /etc/passwd | grep root
    register: command_output
   #register is used here command executes and stores in command_output
  - name: print the command_output
    ansible.builtin.debug:
      msg: "{{ command_output }}"

  - name: redirect the output
    ansible.builtin.command: ls -ltr > /tmp/output.txt
    register: redirect_output
    ignore_errors: true
    #by default ansible stops here but when you use ignore_errors: true it proceed further

  - name: print redirect_output
    ansible.builtin.debug:
      msg: "{{ redirect_output }}"

  - name: redirect the output using shell
    ansible.builtin.shell: ls -ltr > /tmp/output.txt
    register: redirect_output

  - name: print redirect_output shell
    ansible.builtin.debug:
      msg: "{{ redirect_output }}"

  

#   Jinja2 Filters (like functions)

# Filters modify or process variables.

# Example:

# {{ variable | filter }}